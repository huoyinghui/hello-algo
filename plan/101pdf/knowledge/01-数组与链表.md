# 📚 数组与链表

> 🐍 Python专属 | 最基础也最重要的数据结构

---

## 📖 目录

1. [数组 Array](#一数组-array)
2. [链表 Linked List](#二链表-linked-list)
3. [经典题目](#三经典题目)
4. [Python技巧](#四python技巧)

---

## 一、数组 Array

### 1.1 Python列表特性🐍

```python
# Python的list是动态数组
nums = [1, 2, 3, 4, 5]

# 基本操作
nums.append(6)        # O(1) 尾部添加
nums.insert(0, 0)     # O(n) 插入到指定位置
nums.pop()            # O(1) 删除尾部
nums.pop(0)           # O(n) 删除指定位置
nums.remove(3)        # O(n) 删除指定值
nums[2] = 10          # O(1) 修改
len(nums)             # O(1) 长度

# 切片操作🐍
nums[1:3]             # [2, 3] 切片
nums[::-1]            # 反转数组
nums[::2]             # 取偶数位
```

### 1.2 数组常见技巧

#### 📌 双指针

```python
# 删除重复项（有序数组）
def remove_duplicates(nums):
    if not nums:
        return 0
    
    slow = 0
    for fast in range(1, len(nums)):
        if nums[fast] != nums[slow]:
            slow += 1
            nums[slow] = nums[fast]
    
    return slow + 1

# 移动零
def move_zeroes(nums):
    slow = 0
    for fast in range(len(nums)):
        if nums[fast] != 0:
            nums[slow], nums[fast] = nums[fast], nums[slow]
            slow += 1
```

#### 📌 前缀和

```python
# 区间和查询
class NumArray:
    def __init__(self, nums):
        self.prefix = [0] * (len(nums) + 1)
        for i in range(len(nums)):
            self.prefix[i+1] = self.prefix[i] + nums[i]
    
    def sum_range(self, left, right):
        return self.prefix[right+1] - self.prefix[left]
```

#### 📌 二分查找

```python
# 基础二分
def binary_search(nums, target):
    left, right = 0, len(nums) - 1
    
    while left <= right:
        mid = left + (right - left) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    
    return -1
```

### 1.3 经典数组题

#### 📌 两数之和🔥

```python
def two_sum(nums, target):
    seen = {}  # 值 -> 索引🐍
    for i, num in enumerate(nums):
        complement = target - num
        if complement in seen:
            return [seen[complement], i]
        seen[num] = i
    return []
```

#### 📌 合并两个有序数组

```python
def merge(nums1, m, nums2, n):
    # 从后往前填充🔥
    i, j, k = m - 1, n - 1, m + n - 1
    
    while i >= 0 and j >= 0:
        if nums1[i] > nums2[j]:
            nums1[k] = nums1[i]
            i -= 1
        else:
            nums1[k] = nums2[j]
            j -= 1
        k -= 1
    
    # 填充剩余的nums2
    nums1[:j+1] = nums2[:j+1]
```

---

## 二、链表 Linked List

### 2.1 链表节点定义

```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next
```

### 2.2 链表常见技巧

#### 📌 虚拟头节点🔥

```python
# 删除链表中的节点
def delete_node_with_value(head, val):
    dummy = ListNode(0)
    dummy.next = head
    prev = dummy
    curr = head
    
    while curr:
        if curr.val == val:
            prev.next = curr.next
        else:
            prev = curr
        curr = curr.next
    
    return dummy.next
```

#### 📌 快慢指针🔥

```python
# 找链表中点
def find_middle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
    return slow

# 判断环
def has_cycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow == fast:
            return True
    return False
```

#### 📌 反转链表🔥

```python
# 迭代反转
def reverse_list(head):
    prev = None
    curr = head
    
    while curr:
        next_node = curr.next
        curr.next = prev
        prev = curr
        curr = next_node
    
    return prev

# 递归反转
def reverse_list_recursive(head):
    if not head or not head.next:
        return head
    
    new_head = reverse_list_recursive(head.next)
    head.next.next = head
    head.next = None
    
    return new_head

# 反转前N个节点
def reverse_n(head, n):
    if n == 1:
        return head
    
    new_head = reverse_n(head.next, n - 1)
    successor = head.next.next
    head.next.next = head
    head.next = successor
    
    return new_head
```

### 2.3 经典链表题

#### 📌 合并两个有序链表

```python
def merge_two_lists(l1, l2):
    dummy = ListNode(0)
    curr = dummy
    
    while l1 and l2:
        if l1.val < l2.val:
            curr.next = l1
            l1 = l1.next
        else:
            curr.next = l2
            l2 = l2.next
        curr = curr.next
    
    curr.next = l1 or l2
    return dummy.next
```

#### 📌 删除倒数第N个节点🔥

```python
def remove_nth_from_end(head, n):
    dummy = ListNode(0)
    dummy.next = head
    fast = slow = dummy
    
    # fast先走n步
    for _ in range(n):
        fast = fast.next
    
    # 同时走
    while fast.next:
        fast = fast.next
        slow = slow.next
    
    # 删除slow.next
    slow.next = slow.next.next
    return dummy.next
```

#### 📌 链表排序🔥

```python
# 归并排序链表
def sort_list(head):
    if not head or not head.next:
        return head
    
    # 找中点
    slow, fast = head, head.next
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
    
    # 分割
    mid = slow.next
    slow.next = None
    
    # 递归排序
    left = sort_list(head)
    right = sort_list(mid)
    
    # 合并
    return merge_two_lists(left, right)
```

---

## 三、经典题目

### 3.1 数组题目

| 题号 | 题目 | 难度 | 核心技巧 |
|------|------|------|---------|
| 1 | 两数之和 | ⭐ | 哈希表🔥 |
| 26 | 删除有序数组中的重复项 | ⭐ | 双指针 |
| 27 | 移除元素 | ⭐ | 双指针 |
| 88 | 合并两个有序数组 | ⭐ | 从后往前🔥 |
| 283 | 移动零 | ⭐ | 双指针 |
| 303 | 区域和检索 | ⭐ | 前缀和 |

### 3.2 链表题目

| 题号 | 题目 | 难度 | 核心技巧 |
|------|------|------|---------|
| 206 | 反转链表 | ⭐ | 迭代/递归🔥 |
| 21 | 合并两个有序链表 | ⭐ | 双指针 |
| 141 | 环形链表 | ⭐ | 快慢指针🔥 |
| 142 | 环形链表II | ⭐⭐ | 快慢指针 |
| 19 | 删除链表的倒数第N个节点 | ⭐⭐ | 快慢指针🔥 |
| 148 | 排序链表 | ⭐⭐ | 归并排序 |
| 234 | 回文链表 | ⭐ | 快慢指针+反转 |

---

## 四、Python技巧

### 4.1 列表推导式🐍

```python
# 生成平方数
squares = [x**2 for x in range(10)]

# 过滤偶数
evens = [x for x in nums if x % 2 == 0]

# 二维数组初始化
matrix = [[0] * n for _ in range(m)]  # 正确✅
matrix = [[0] * n] * m                # 错误❌ 浅拷贝
```

### 4.2 常用内置函数🐍

```python
# enumerate - 获取索引和值
for i, val in enumerate(nums):
    print(f'{i}: {val}')

# zip - 打包多个列表
for a, b in zip(list1, list2):
    print(a, b)

# sorted - 排序（不改变原数组）
sorted_nums = sorted(nums)
sorted_nums = sorted(nums, reverse=True)  # 降序
sorted_nums = sorted(nums, key=lambda x: abs(x))  # 自定义

# min/max with key
min_val = min(nums, key=lambda x: abs(x))
```

### 4.3 切片技巧🐍

```python
# 反转
reversed_nums = nums[::-1]

# 复制
copy_nums = nums[:]

# 删除前n个
nums = nums[n:]

# 删除后n个
nums = nums[:-n]

# 步长为2
even_indices = nums[::2]
odd_indices = nums[1::2]
```

### 4.4 collections模块🐍

```python
from collections import deque, Counter, defaultdict

# deque - 双端队列
dq = deque([1, 2, 3])
dq.append(4)       # 右端添加
dq.appendleft(0)   # 左端添加
dq.pop()           # 右端删除
dq.popleft()       # 左端删除

# Counter - 计数
counter = Counter([1, 2, 2, 3, 3, 3])
most_common = counter.most_common(2)  # [(3, 3), (2, 2)]

# defaultdict - 默认字典
graph = defaultdict(list)
graph[1].append(2)  # 自动创建空列表
```

---

## 📝 学习建议

### 🎯 重点掌握

**数组**:
- ✅ 双指针技巧（快慢指针、左右指针）
- ✅ 前缀和
- ✅ 二分查找
- ✅ 列表推导式🐍

**链表**:
- ✅ 虚拟头节点技巧
- ✅ 快慢指针（找中点、判断环）
- ✅ 反转链表（迭代和递归）
- ✅ 双指针合并

### 🔥 刷题顺序

1. **第1-2天**: 数组双指针（26, 27, 283）
2. **第3-4天**: 数组基础题（1, 88, 303）
3. **第5-6天**: 链表基础（206, 21, 141）
4. **第7-8天**: 链表进阶（142, 19, 148, 234）

### 🐍 Python特色

```python
# 1. 列表推导式生成数组
result = [x * 2 for x in nums if x > 0]

# 2. 多变量赋值交换
a, b = b, a

# 3. 切片反转
nums = nums[::-1]

# 4. enumerate获取索引
for i, val in enumerate(nums):
    # ...

# 5. deque实现O(1)的双端操作
from collections import deque
dq = deque()
dq.appendleft(1)  # O(1)
dq.popleft()      # O(1)
```

---

## 🔗 相关文件

- 📊 [基础数据结构脑图](../mindmaps/01-基础数据结构脑图.md)
- 📖 [双指针技巧](../algorithms/双指针技巧.md)
- 💻 [链表操作模板](../templates/链表操作模板.md)
- 📋 [必刷题目清单](../practice/必刷题目清单.md)

**🎯 数组和链表是基础中的基础,必须熟练掌握！**
